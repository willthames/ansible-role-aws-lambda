# ansible-playbook test.yml -vv

- hosts: runner
  max_fail_percentage: 1
  tasks:
    - block:
        - include_role:
            name: setup
        - include_role:
            name: aws-lambda
      always:
        - include_role:
            name: teardown
